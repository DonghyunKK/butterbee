<%= simple_form_for(cupcake) do |f| %>
  <%= f.input :name %>
  <%= f.input :item_code %>
  <%= f.input :seasonal, :as => :boolean %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit %>
<% end %>